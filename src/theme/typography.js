// eslint-disable-next-line no-unused-vars
const typography = (palette) => ({
    fontFamily: [
        'Arial',
        'Helvetica',
        'sans-serif',
    ],
    fontSize: 12,
})

export default typography;